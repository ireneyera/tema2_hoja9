<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arey.es.tema2_hoja9">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityDestino" />
        <activity android:name=".Ejercicio1" />
        <activity android:name=".Ejercicio2" />
        <activity android:name=".Ejercicio3" />
        <activity android:name=".Ejercicio4" />

        <receiver
            android:name=".ReceiverEjercicio4"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="tema2_hoja9.ACCION"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>